{{#if hasBlock}}
  {{yield
    (hash
      anyFilterUsed=anyFilterUsed
      summary=summary
      clearFilters=doClearFilters
      firstIndex=firstIndex
      lastIndex=lastIndex
      recordsCount=recordsCount
    )}}
{{else}}
  {{#bs-form
    formLayout="inline"
    model=this
    onSubmit=(action "noop")}}
    <label>{{summary}}</label>
    {{#bs-button
      class=(concat "clearFilters border-0 " themeInstance.buttonLink)
      disabled=(unless anyFilterUsed "disabled")
      type="bytton"
      onClick=(action "doClearFilters")
      buttonType="secondary"}}
      <i class={{themeInstance.clearAllFiltersIcon}}></i>
    {{/bs-button}}
  {{/bs-form}}
{{/if}}
