<PaperCheckbox
  @indeterminate={{and this.selectedItems.length (not-eq this.selectedItems.length this.data.length)}}
  @value={{is-equal this.selectedItems.length this.data.length}}
  @onChange={{this.doToggleAllSelection}} />
{{yield}}