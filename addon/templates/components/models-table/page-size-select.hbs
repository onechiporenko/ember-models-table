{{#with
  (hash
    Select=(
      component themeInstance.components.select
      options=pageSizeOptions
      value=pageSize
      type=type
      themeInstance=themeInstance
      class="changePageSize"
    )
    themeInstance=themeInstance
    pageSizeOptions=pageSizeOptions
    pageSize=pageSize
  )
as |PageSizeSelect|}}
  {{#if hasBlock}}
    {{yield PageSizeSelect}}
  {{else}}
    <form class={{themeInstance.form}}>
      <div class={{themeInstance.formElementWrapper}}>
        <label>{{themeInstance.messages.rowsCount}}</label>
        <PageSizeSelect.Select/>
      </div>
    </form>
  {{/if}}
{{/with}}
