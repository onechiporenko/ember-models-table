import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('models-table/row-grouping', 'Integration | Component | models table/row grouping', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
});
