import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('models-table/themes/semanticui/data-group-by-select', 'Integration | Component | models table/themes/semanticui/data group by select', {
  integration: true
});

test('it renders', function(assert) {
  assert.ok(true);
});
