import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('models-table/data-group-by-select', 'Integration | Component | models table/data group by select', {
  integration: true
});

test('it renders', function(assert) {

  assert.ok(true);
});
