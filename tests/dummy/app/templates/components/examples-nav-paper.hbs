{{#paper-tabs selected=currentRouteName borderBottom=true onChange=(action "noop") as |tabs|}}
  {{#each routes as |route|}}
    {{#tabs.tab value=route.route href=(href-to route.route)}}
      {{route.name}}
    {{/tabs.tab}}
  {{/each}}
{{/paper-tabs}}

{{yield}}