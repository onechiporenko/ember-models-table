<a href="#" {{action "toggleGroupedRows"}}>{{currentGroupingPropertyName}}: {{groupedValue}} ({{groupedLength}}). {{if groupIsCollapsed "collapsed" "expanded"}}</a> <button class="toggle-selection-group" onclick={{action "toggleGroupedRowsSelection"}}></button> <button class="toggle-expands-group" onclick={{action "toggleGroupedRowsExpands"}}></button>