<this.BsNav @type="tabs" as |nav|>
  {{#each @routes as |route|}}
    <nav.item>
      {{#nav.link-to route.route}}
        {{#if route.icon}}
          <i class={{route.icon}}></i>
        {{/if}}
        {{route.name}}
      {{/nav.link-to}}
    </nav.item>
  {{/each}}
</this.BsNav>